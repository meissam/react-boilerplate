import React from 'react';
import { Header } from '@components/layout';

// App.js sets up the auth and routing.
export const App = () => {
    return (
        <h1 className="app">
            This is asdf a asdfadsf <Header /> and its
        </h1>
    );
};
